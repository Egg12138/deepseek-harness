/**
 * Web floating whale plugin, node half.
 *
 * Deliberately empty. The whale is a browser-only frame overlay: it composes
 * no host capability, publishes no service, and never reaches the agent plane.
 */

/** Host plugin body — the browser half owns the entire surface. */
export function apply(): void {}
